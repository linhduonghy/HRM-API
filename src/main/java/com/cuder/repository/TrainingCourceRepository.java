package com.cuder.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.cuder.model.TrainingCourse;

public interface TrainingCourceRepository extends JpaRepository<TrainingCourse, Integer>{

}
